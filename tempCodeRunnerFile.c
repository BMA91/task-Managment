
          while (exit<0 || exit>7)
          {
            printf("you must enter a number between(1-7)\ntry again:");
            scanf("%d",&exit);
          }  
      } while (exit<0 || exit>7 );

      switch (exit)
      {